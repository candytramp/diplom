= form_for @scientific_school do |f|
  - if @scientific_school.errors.any?
    #error_explanation
      %h2= "#{pluralize(@scientific_school.errors.count, "error")} prohibited this scientific_school from being saved:"
      %ul
        - @scientific_school.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :creator_login
    = f.text_field :creator_login
  .field
    = f.label :creator_data
    = f.text_area :creator_data
  .actions
    = f.submit 'Save'
